<div class="navbar-inner">
  <div class="left">
    {{#if f7.iosTheme}}
      {{#link-to 'index' class="back link"}}
        <i class="icon icon-back"></i><span>Back</span>
      {{/link-to}}
    {{else}}
      {{#link-to 'index' class="back link icon-only"}}
        <i class="icon icon-back"></i>
      {{/link-to}}
    {{/if}}
  </div>
  <div class="center">{{item.title}}</div>
</div>
