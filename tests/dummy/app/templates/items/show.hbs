{{partial 'material-navbar'}}

{{#f7-page-content}}
  <div class="card">
    <div class="card-header">{{item.title}}</div>
    <div class="card-content">
      <div class="card-content-inner">{{item.description}}</div>
    </div>
    <div class="card-footer">Count: {{item.count}}</div>
  </div>
{{/f7-page-content}}